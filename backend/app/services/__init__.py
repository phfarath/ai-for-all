"""Business logic services layer.

This module will contain service classes that encapsulate
business logic for different domains (users, courses, lessons, etc).

Services should be stateless and depend on database sessions
passed as arguments, promoting testability and modularity.
"""
